# ============================================================
# requirements-minimal.txt — Dự án Quản lý Giao thông Lào Cai
# Phiên bản: MINIMAL (chỉ direct dependencies, không ghim version)
# Python: 3.10+
# Cài đặt: pip install -r requirements-minimal.txt
# ============================================================
# Dùng file này khi muốn cài bản mới nhất của từng thư viện,
# hoặc khi triển khai trên môi trường có Python version khác.
# ============================================================

openpyxl>=3.1.0     # Đọc/ghi file Excel (.xlsx)
pandas>=2.0.0       # Xử lý & phân tích dữ liệu bảng
matplotlib>=3.7.0   # Vẽ biểu đồ
numpy>=1.24.0       # Tính toán số học (dependency của pandas/matplotlib)
