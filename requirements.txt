# ============================================================
# requirements.txt — Dự án Quản lý Giao thông Lào Cai
# Phiên bản: PINNED (đóng băng version cụ thể)
# Python: 3.10+
# Cài đặt: pip install -r requirements.txt
# Cập nhật: 2025-03-25
# ============================================================

# ── DIRECT DEPENDENCIES (thư viện dùng trực tiếp trong code) ──

# Đọc/ghi file Excel (.xlsx) — dùng trong tools/excel_to_data.py và he_thong_route.py
openpyxl==3.1.5

# Xử lý & phân tích dữ liệu dạng bảng — dùng trong báo cáo console (main.py)
pandas==2.3.3

# Vẽ biểu đồ, đồ thị — dùng trong báo cáo console
matplotlib==3.10.8

# Tính toán số học / mảng dữ liệu (dependency của pandas & matplotlib)
numpy==2.2.6

# Framework xây dựng REST API & web app (async, tự động sinh OpenAPI docs)
fastapi==0.115.12

# ASGI server — chạy ứng dụng FastAPI (development: uvicorn api.main:app --reload)
uvicorn==0.34.0

# Template engine HTML — render giao diện web Jinja2
jinja2==3.1.6

# Parse multipart/form-data — bắt buộc khi FastAPI nhận upload file hoặc form HTML
python-multipart==0.0.22

# WSGI server production — chạy nhiều worker process (Linux VPS only, không chạy trên Windows)
# Dùng: gunicorn -c gunicorn.conf.py api.main:app
gunicorn==21.2.0

# Đọc biến môi trường từ file .env
python-dotenv==1.2.2

# Rate limiting — giới hạn số request/phút để chống brute-force đăng nhập
slowapi==0.1.9

# Băm & xác minh mật khẩu an toàn (bcrypt algorithm)
bcrypt==5.0.0

# ── TRANSITIVE DEPENDENCIES (kéo theo tự động, ghi rõ để đảm bảo tái tạo môi trường) ──

# >> Nhóm fastapi / starlette
starlette==0.46.1          # ASGI micro-framework nền tảng của FastAPI
pydantic==2.11.3           # Validation dữ liệu
pydantic-core==2.33.1      # Lõi C của Pydantic
annotated-types==0.7.0     # Hỗ trợ Annotated type hints
anyio==4.9.0               # Async I/O layer (backend cho Starlette)
sniffio==1.3.1             # Phát hiện async runtime đang dùng
idna==3.10                 # Xử lý tên miền quốc tế hoá
typing_extensions==4.15.0  # Backport type hints (Python < 3.12)
typing-inspection==0.4.2   # Introspection cho type hints (dep của pydantic)
exceptiongroup==1.2.2      # Backport ExceptionGroup (Python < 3.11)

# >> Nhóm uvicorn
click==8.1.8               # CLI parsing (dùng bởi uvicorn & pip)
h11==0.16.0                # HTTP/1.1 parser thuần Python
colorama==0.4.6            # Màu terminal (dep của click trên Windows)

# >> Nhóm jinja2
MarkupSafe==3.0.2          # Xử lý chuỗi HTML an toàn (dep của Jinja2)

# >> Nhóm slowapi / limits
limits==5.8.0              # Rate limit storage backends (dep của slowapi)
Deprecated==1.3.1          # Decorator @deprecated (dep của limits)
wrapt==2.1.2               # Function wrapping (dep của Deprecated)

# >> Nhóm pandas / matplotlib
contourpy==1.3.2
cycler==0.12.1
et_xmlfile==2.0.0
fonttools==4.61.1
kiwisolver==1.4.9
packaging==26.0
pillow==12.1.1
pyparsing==3.3.2
python-dateutil==2.9.0.post0
pytz==2026.1.post1
six==1.17.0
tzdata==2025.3

# >> Nhóm httpx (dep của một số lib, dùng trong tests nếu cần)
certifi==2026.2.25         # SSL certificates bundle
httpcore==1.0.9            # HTTP client core (dep của httpx)
httpx==0.28.1              # HTTP client async (dùng bởi httpcore)

# ============================================================
# KHÔNG có trong requirements này (không phải dep dự án):
#   anthropic, jiter, distro, docstring_parser — công cụ AI ngoài
#
# Built-in Python (KHÔNG cần cài):
#   sqlite3, os, sys, pathlib, json, importlib,
#   argparse, glob, base64, secrets, math, re, io, datetime
#
# Ghi chú gunicorn:
#   - Trên Windows: chạy uvicorn api.main:app --reload
#   - Trên Linux VPS: gunicorn -c gunicorn.conf.py api.main:app
# ============================================================
